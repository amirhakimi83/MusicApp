package com.example.musicapp.feature.home

import androidx.compose.runtime.Composable
import androidx.compose.ui.Modifier
import com.example.musicapp.R
import com.example.musicapp.ui.components.ComingSoonScreen

@Composable
fun HomeScreen(modifier: Modifier = Modifier) {
    // Full Home implementation lands in Step 7.
    ComingSoonScreen(titleRes = R.string.tab_home, modifier = modifier)
}
