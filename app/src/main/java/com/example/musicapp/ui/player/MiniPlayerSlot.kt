package com.example.musicapp.ui.player

import androidx.compose.runtime.Composable
import androidx.compose.ui.Modifier

/**
 * Placeholder for the persistent mini-player that sits just above the bottom
 * navigation bar. It renders nothing until the media player is wired up in
 * Step 9, at which point it shows the currently playing track.
 */
@Composable
fun MiniPlayerSlot(
    modifier: Modifier = Modifier,
    onExpand: () -> Unit = {},
) {
    // Intentionally empty until Step 9.
}
